import { describe, it } from "bun:test";

/**
 * `move-to-location` moves a character to a location slot, updating the
 * atLocationId meta. Location slot plumbing (cost payment, replacement
 * effects) is covered in the simulator integration tests around Location
 * cards.
 */
describe("move-to-location", () => {
  it.todo("unit: add move-to-location coverage once location-slot plumbing is in the harness");
});
