import { describe, it } from "bun:test";

/**
 * `enable-play-from-under` grants an ability that lets a player play a card
 * from under another card (e.g. Shift's "played cards under"). This wires
 * into the enable/restriction registry, which requires full-engine plumbing
 * to assert end-to-end.
 */
describe("enable-play-from-under", () => {
  it.todo("unit: add enable-play-from-under coverage once the enable-registry harness exists");
});
