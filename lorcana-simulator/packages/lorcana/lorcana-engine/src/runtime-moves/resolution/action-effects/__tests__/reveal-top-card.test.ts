import { describe, it } from "bun:test";

/**
 * `reveal-top-card` reads N cards from the deck top without moving them and
 * stores them in the event snapshot. Uses framework RNG + peek primitives.
 * Coverage lives in simulator integration tests around cards like
 * "Deep Freeze".
 */
describe("reveal-top-card", () => {
  it.todo("unit: add reveal-top-card coverage once deck-peek primitives are in the harness");
});
