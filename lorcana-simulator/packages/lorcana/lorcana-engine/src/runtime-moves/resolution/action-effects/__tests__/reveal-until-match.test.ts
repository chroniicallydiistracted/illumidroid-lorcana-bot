import { describe, it } from "bun:test";

/**
 * `reveal-until-match` reveals cards from the deck top until a matching one
 * is found, then stops. Involves deck-iteration + match predicate; simulator
 * integration tests cover the full flow.
 */
describe("reveal-until-match", () => {
  it.todo(
    "unit: add reveal-until-match coverage once deck-iteration primitives are in the harness",
  );
});
