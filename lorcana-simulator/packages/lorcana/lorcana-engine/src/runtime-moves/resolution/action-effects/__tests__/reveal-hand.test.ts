import { describe, it } from "bun:test";

/**
 * `reveal-hand` marks every card in a player's hand as visible. Uses framework
 * zone iteration + per-card public-face tracking; covered in simulator
 * integration tests for cards like "Queen of Hearts - Sense of Humor".
 */
describe("reveal-hand", () => {
  it.todo("unit: add reveal-hand coverage once hand-reveal primitives are in the harness");
});
