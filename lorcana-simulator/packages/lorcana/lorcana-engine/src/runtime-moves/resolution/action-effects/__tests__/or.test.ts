import { describe, it } from "bun:test";

/**
 * `or` evaluates each child effect's legality under the current state and
 * dispatches to the first valid option. Legality evaluation and dispatch
 * are covered in `../composed-effect-resolver.test.ts`.
 */
describe("or", () => {
  it.todo("covered by composed-effect-resolver.test.ts");
});
