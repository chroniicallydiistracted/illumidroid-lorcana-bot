import { describe, it } from "bun:test";

/**
 * Deeper coverage for `return-random-from-inkwell` lives in the sibling
 * `return-random-from-inkwell-effect.test.ts` file. Per-variant unit
 * coverage is deferred until the RNG + inkwell tracking is wrapped by the
 * unit harness.
 */
describe("return-random-from-inkwell", () => {
  it.todo(
    "unit: add return-random-from-inkwell coverage once RNG + inkwell tracking is in the harness",
  );
});
