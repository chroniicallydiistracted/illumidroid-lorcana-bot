import { describe, it } from "bun:test";

/**
 * `choice` asks the current player to pick one of N child effects. The
 * suspension + chooser-selection flow is exercised in
 * `../composed-effect-resolver.test.ts` and in simulator integration tests
 * covering "choose one" cards.
 */
describe("choice", () => {
  it.todo("covered by composed-effect-resolver.test.ts + simulator integration");
});
