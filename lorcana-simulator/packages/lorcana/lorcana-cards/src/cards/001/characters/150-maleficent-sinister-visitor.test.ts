import { describe, expect, it } from "bun:test";
import { LorcanaTestEngine, PLAYER_ONE } from "@tcg/lorcana-engine/testing";
import { maleficentSinisterVisitor } from "./150-maleficent-sinister-visitor";

describe("Maleficent - Sinister Visitor", () => {
  // Add ability tests here
  // Examples:
  // It("has [Keyword]", () => {
  //   Const testEngine = new LorcanaTestEngine({ play: [maleficentSinisterVisitor] });
  //   Expect(testEngine.getCardModel(maleficentSinisterVisitor).hasKeyword()).toBe(true);
  // });
  // TODO: Add tests for abilities
});
