import { describe, it } from "bun:test";

describe("Static Abilities: Grant Keyword", () => {
  // Static abilities that grant keywords to other characters.
  //
  // Test cases to cover:
  // 1. "Your characters gain Ward" grants Ward to all friendly characters
  // 2. New characters entering play also receive the granted keyword
  // 3. Keyword is removed when the granting card leaves play
  // 4. Conditional keyword grant: "While you have 10+ ink, your characters gain Rush"

  it.todo("should grant keyword to all friendly characters", () => {});

  it.todo("should grant keyword to new characters entering play", () => {});

  it.todo("should remove granted keyword when source card leaves play", () => {});

  it.todo("should only grant keyword while condition is met", () => {});
});
