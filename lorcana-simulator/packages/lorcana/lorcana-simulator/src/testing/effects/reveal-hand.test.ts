import { describe, it } from "bun:test";

describe("Reveal Hand - Reveals all cards in a player's hand", () => {
  // Effect type(s): reveal-hand-effect
  //
  // Test cases to cover:
  // 1. Reveals opponent's hand cards
  // 2. No-op when target hand is empty
  // 3. Targets correct player via player resolver

  it.todo("should reveal opponent's hand cards", () => {});

  it.todo("should be a no-op when the target hand is empty", () => {});
});
