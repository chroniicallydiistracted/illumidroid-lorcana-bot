import { describe, expect, it } from "bun:test";
import { LorcanaTestEngine, PLAYER_ONE } from "@tcg/lorcana-engine/testing";
import { scarFieryUsurper } from "./122-scar-fiery-usurper";

describe("Scar - Fiery Usurper", () => {
  // Add ability tests here
  // Examples:
  // It("has [Keyword]", () => {
  //   Const testEngine = new LorcanaTestEngine({ play: [scarFieryUsurper] });
  //   Expect(testEngine.getCardModel(scarFieryUsurper).hasKeyword()).toBe(true);
  // });
  // TODO: Add tests for abilities
});
