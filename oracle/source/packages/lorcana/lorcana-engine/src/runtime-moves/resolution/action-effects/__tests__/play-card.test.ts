import { describe, it } from "bun:test";

/**
 * `play-card` plays a card from a scry destination or hand, paying its cost
 * and executing its abilities. Deeply intertwined with the full play-card
 * move; covered in simulator integration tests.
 */
describe("play-card", () => {
  it.todo("covered by simulator integration tests (full play-card flow)");
});
