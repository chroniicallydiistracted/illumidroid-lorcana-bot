import { describe, it } from "bun:test";

/**
 * `reveal-and-route` reveals cards then evaluates per-card conditions to
 * dispatch a routing effect. The route matrix is complex; covered via
 * `../composed-effect-resolver.test.ts` and simulator integration tests.
 */
describe("reveal-and-route", () => {
  it.todo("covered by composed-effect-resolver.test.ts + simulator integration");
});
