import { describe, it } from "bun:test";

/**
 * `pay-cost` (aliased as `cost-effect`) validates and executes a sacrificial
 * cost (ink, exert, discard). Suspension + cost-validation plumbing is
 * covered via the full play-card flow in simulator integration tests.
 */
describe("pay-cost", () => {
  it.todo("covered by simulator integration tests (full cost-payment flow)");
});
