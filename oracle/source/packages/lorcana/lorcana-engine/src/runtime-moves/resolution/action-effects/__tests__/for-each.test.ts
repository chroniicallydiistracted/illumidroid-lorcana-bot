import { describe, it } from "bun:test";

/**
 * `for-each` multiplies a child effect by a counter value resolved from
 * `resolutionInput.amount` or an amount expression. Counter threading is
 * covered in `../composed-effect-resolver.test.ts` and
 * `../amount-resolver.test.ts`.
 */
describe("for-each", () => {
  it.todo("covered by composed-effect-resolver.test.ts + amount-resolver.test.ts");
});
