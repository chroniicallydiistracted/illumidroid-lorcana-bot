import { describe, it } from "bun:test";

/**
 * `draw-until-hand-size` draws cards repeatedly until the controller's hand
 * reaches a target size. Uses the same draw plumbing as `draw`; end-to-end
 * flow is covered via simulator integration tests.
 */
describe("draw-until-hand-size", () => {
  it.todo("unit: add draw-until-hand-size coverage once hand-size tracking is in the harness");
});
