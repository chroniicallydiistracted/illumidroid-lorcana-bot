import { describe, it } from "bun:test";

/**
 * `put-under` stacks a card beneath another (Shift / boost mechanic). The
 * stack manipulation uses framework primitives that the unit harness does
 * not yet model; simulator integration tests cover the mechanic end-to-end.
 */
describe("put-under", () => {
  it.todo("unit: add put-under coverage once the stack primitives are in the harness");
});
