import { describe, it } from "bun:test";

/**
 * `scry` suspends for a chooser to arrange revealed cards across destination
 * zones. Ordering logic is unit-tested in `../scry-ordering.test.ts`;
 * end-to-end flow is covered in simulator integration tests.
 */
describe("scry", () => {
  it.todo("covered by scry-ordering.test.ts + simulator integration");
});
