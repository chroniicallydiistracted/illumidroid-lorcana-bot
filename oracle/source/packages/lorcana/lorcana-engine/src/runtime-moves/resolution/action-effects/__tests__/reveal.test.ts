import { describe, it } from "bun:test";

/**
 * `reveal` sets the selected cards as publicly visible and writes them into
 * the event snapshot for downstream condition / routing logic. Covered by
 * `../composed-effect-resolver.test.ts` and simulator "reveal" tests.
 */
describe("reveal", () => {
  it.todo("covered by composed-effect-resolver.test.ts");
});
