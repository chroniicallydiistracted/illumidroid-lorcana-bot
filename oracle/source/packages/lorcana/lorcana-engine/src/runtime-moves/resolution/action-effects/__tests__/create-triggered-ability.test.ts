import { describe, it } from "bun:test";

/**
 * `create-triggered-ability` registers a delayed / floating triggered ability
 * scoped to the given duration. Triggered-ability registry plumbing is
 * covered via simulator integration tests and
 * `../../../triggered-abilities/` unit tests.
 */
describe("create-triggered-ability", () => {
  it.todo("covered by triggered-abilities/* tests + simulator integration");
});
