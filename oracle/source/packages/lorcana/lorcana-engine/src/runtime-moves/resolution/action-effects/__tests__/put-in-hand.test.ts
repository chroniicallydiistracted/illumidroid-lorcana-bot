import { describe, it } from "bun:test";

/**
 * `put-in-hand` moves a resolved target into its owner's hand. The resolver
 * handles multiple source zones (deck, discard, inkwell) and emits triggered
 * events that rely on framework plumbing.
 */
describe("put-in-hand", () => {
  it.todo("unit: add put-in-hand coverage once multi-source zone tracking is in the harness");
});
