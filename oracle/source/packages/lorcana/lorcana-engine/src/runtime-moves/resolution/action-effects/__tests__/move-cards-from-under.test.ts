import { describe, it } from "bun:test";

/**
 * `move-cards-from-under` unstacks cards from under a parent and moves them to
 * a destination zone. Depends on stack primitives and trigger emission — see
 * simulator integration tests.
 */
describe("move-cards-from-under", () => {
  it.todo("unit: add move-cards-from-under coverage once stack primitives are in the harness");
});
